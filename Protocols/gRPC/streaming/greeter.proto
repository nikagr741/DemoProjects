syntax = "proto3";

package greeter;

service Greeter {
  // Server-side streaming: Client sends 1 request, Server returns a stream
  rpc StreamGreetings (HelloRequest) returns (stream HelloReply) {}
}

message HelloRequest {
  string name = 1;
}

message HelloReply {
  string message = 1;
}